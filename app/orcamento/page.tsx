'use client';
import {useState} from 'react';import {createClient} from '../../lib/supabase';
export default function Orcamento(){const [msg,setMsg]=useState('');async function enviar(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const {error}=await createClient().from('orcamentos').insert({nome:f.get('nome'),whatsapp:f.get('whatsapp'),bairro:f.get('bairro'),descricao:f.get('descricao')});setMsg(error?'Erro: '+error.message:'Pedido enviado. Agora ele já está salvo no PONTE.AI.');if(!error)e.currentTarget.reset()}return <main><section className="formbox"><div className="pill">ORÇAMENTO</div><h1>Do que você precisa?</h1><form onSubmit={enviar}><label>Seu nome<input name="nome" required/></label><label>WhatsApp<input name="whatsapp" required/></label><label>Bairro<input name="bairro"/></label><label>Serviço<textarea name="descricao" required/></label><button>Enviar solicitação</button></form>{msg&&<p>{msg}</p>}</section></main>}
